import { Building2 } from 'lucide-react';
import type { SVGAttributes } from 'react';

export default function OperisLogoIcon(props: SVGAttributes<SVGElement>) {
    return (
        <Building2 {...props} />
    );
}